# OpenAI API key for live LLM reconciliation review.
# Required for AI review. Without it, ambiguous records are safely held as exceptions.
OPENAI_API_KEY=

# LLM model specification
AI_MODEL=gpt-4.1-mini
